#include <stdio.h>
#include <string.h>

int main() {
    char word[] = "yellow";
    int start = 3;
    int length = 3;
    char result[100]; 
    strncpy(result, word + start, length);
    result [length] = '\0';
    printf("Результат: %s\n", result);
    return 0;
}
